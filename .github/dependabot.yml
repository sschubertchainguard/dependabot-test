version: 2
registries:
  chainguard: # Define access for a private registry
    type: docker-registry
    url: cgr.dev
    key: ${{secrets.PULL_TOKEN_ID}}
    token: ${{secrets.PULL_TOKEN_PW}}
updates:
  - package-ecosystem: "docker"
    directory: "/"
    registries:
      - chainguard
    schedule:
      interval: "daily"
    open-pull-requests-limit: 5
